export enum STATUS {
  SYNCED_REMOTE = "synced-remote",
}

export enum CONNECTION_STATUS {
  ONLINE = "online",
  OFFLINE = "offline",
}

export enum CHANGE_TYPE {
  ADD = "add",
  UPDATE = "update",
  DELETE = "delete",
}
